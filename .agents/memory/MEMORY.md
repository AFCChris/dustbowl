# Memory index
- [Race lap gating & countdown timers](race-lap-gating.md) — riders spawning behind start/finish need the sectorSeen gate on every lap-detection path; tokenize countdown timers.
- [Headless smoke testing](headless-smoke-testing.md) — preview browser has no WebGL; verify via `node tools/smoke.js`, which drives all courses through `__dbg` in a vm sandbox.
